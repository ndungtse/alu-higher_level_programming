# Python - Everything is object

Answers and code exploring how Python treats objects: `id` and `type`, identity
vs equality, mutable vs immutable objects, aliasing, and how arguments are passed
to functions.
